# ─── Entorno virtual y variables de entorno ───────────────────────
venv/
.env
.env.local
.env.*.local
.python-version

# Sugerencia: sí comitear un .env.example con valores ficticios
# .env.example

# ─── Python compilado y caché ────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class

# ─── Artefactos de empaquetado Python ────────────────────────────
build/
dist/
*.egg-info/

# ─── Sistema operativo ───────────────────────────────────────────
.DS_Store
Thumbs.db

# ─── Bases de datos y logs ───────────────────────────────────────
*.sqlite3
db.sqlite3
*.log
logs/
backup/

# ─── Celery / Redis (runtime) ────────────────────────────────────
celerydb.sqlite
celerybeat-schedule
dump.rdb
*.rdb

# ─── Backups y ediciones temporales ─────────────────────────────
*.bak
*.swp
*.swo
*.tmp
tmp/

# ─── Configuraciones locales o secretas ─────────────────────────
*.local
*.secret
*.pem
*.key
*.crt
*.p12

# ─── Media y estáticos generados ────────────────────────────────
media/

# IMPORTANTE: deja solo el STATIC_ROOT que uses realmente en settings.py
# Si usas 'staticfiles' como STATIC_ROOT:
staticfiles/
# Si usas 'static_root' como STATIC_ROOT:
# static_root/
# Si usas 'collected_static' como STATIC_ROOT:
# collected_static/

# Opcional: ignorar salidas compiladas de Tailwind/PostCSS si las generas
# (ajusta las rutas a tu proyecto)
**/static/**/output.css
**/static/**/output.css.map

# ─── Frameworks front-end ────────────────────────────────────────
node_modules/
public/build/
# Otras caches de bundlers opcionales:
# .parcel-cache/
# .next/
# .vite/

# ─── IDEs y editores ─────────────────────────────────────────────
.vscode/
.idea/
*.code-workspace

# ─── Scripts propios y exportaciones ────────────────────────────
# Mantén ignorados CSV sensibles o masivos en rutas específicas, no globales
z_mis_script/000/HotelImportado*.csv
exports/
documentos/

# ─── Archivos de pruebas temporales (scope por carpetas) ────────
# Evita ignorar globalmente XML/CSV/XLS; limítalo a carpetas temporales.
# Si necesitas ignorarlos en tmp/ o exports/, ya están arriba:
# tmp/*.xml
# tmp/*.csv
# tmp/*.xls
# tmp/*.xlsx

# ─── Caches y reportes de linters/tests ──────────────────────────
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.mypy_cache/
.ruff_cache/

# ─── Jupyter/Notebooks ───────────────────────────────────────────
.ipynb_checkpoints/

# ─── Otros archivos sueltos que no deberían versionarse ─────────
get-pip.py